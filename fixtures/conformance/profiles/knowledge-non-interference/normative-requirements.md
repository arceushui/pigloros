# Knowledge non-interference profile requirements

For every declared ADR-059 coordinate, the evaluator must compare public
outputs produced from inputs that differ only in prohibited knowledge. It must
reject any output, ordering, diagnostic, or state difference outside the
coordinate's declared equality relation. Unexecuted Draft coordinates carry no
result claim.
